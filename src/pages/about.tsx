import * as React from "react";
import Layout from "../components/layout";

const AboutPage = () => {
  return (
    <Layout pageTitle="Über mich">
      <h2>...wird viel erzählt</h2>
    </Layout>
  );
};

// Step 3: Export your component
export default AboutPage;
